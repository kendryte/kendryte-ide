import { localize } from 'vs/nls';

export const ACTION_ID_UPGRADE_BUILDING_BLOCKS = 'workbench.action.kendryte.packageUpgrade';
export const ACTION_LABEL_UPGRADE_BUILDING_BLOCKS = localize('packageManager.upgrade.building', 'Update required packages');